{
  "name": "@nocturne/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup --config ../../tsup.app.config.ts",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@nocturne/ai-gm": "workspace:*",
    "@nocturne/auth": "workspace:*",
    "@nocturne/content-engine": "workspace:*",
    "better-auth": "^1.4.0",
    "fastify": "^5.0.0",
    "pg": "^8.13.0",
    "zod": "^4.0.0"
  }
}
