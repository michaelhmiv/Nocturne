{
  "name": "@nocturne/ai-gm",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:provider-contract": "tsx src/provider-contract.ts"
  },
  "dependencies": {
    "@nocturne/contracts": "workspace:*",
    "zod": "^4.0.0"
  }
}
