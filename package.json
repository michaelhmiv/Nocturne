{
  "name": "nocturne",
  "private": true,
  "packageManager": "pnpm@11.17.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build --concurrency=1 && pnpm build:mcp",
    "build:mcp": "tsup apps/mcp/src/index.ts --format cjs --clean --out-dir apps/mcp/dist && node scripts/ci/mcp-bundle-smoke.mjs",
    "dev": "turbo run dev --parallel",
    "dev:mcp": "tsx watch apps/mcp/src/index.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "turbo run typecheck --concurrency=2 && tsc --noEmit -p apps/mcp/tsconfig.json",
    "test": "turbo run test --concurrency=1 && vitest run test/mcp-server.test.ts test/mcp-account-binding.test.ts test/mcp-oauth-durability.test.ts test/mcp-operational-health.test.ts test/mcp-action-corpus.test.ts",
    "test:mcp": "vitest run test/mcp-server.test.ts test/mcp-account-binding.test.ts test/mcp-oauth-durability.test.ts test/mcp-operational-health.test.ts test/mcp-action-corpus.test.ts",
    "test:certification": "vitest run test/capabilities test/telemetry test/action-matrix",
    "test:action-matrix": "vitest run test/action-matrix/action-matrix.test.ts",
    "test:capabilities": "vitest run test/capabilities",
    "test:telemetry": "vitest run test/telemetry apps/api/src/world-action-handler-registry.test.ts",
    "verify": "pnpm format:check && pnpm typecheck && pnpm test && pnpm test:certification && pnpm build",
    "ci:static": "pnpm format:check && pnpm typecheck && pnpm test && pnpm test:certification && pnpm build",
    "db:migrate": "pnpm --filter @nocturne/database db:migrate",
    "db:check": "pnpm --filter @nocturne/database db:check",
    "world:grant-role": "pnpm --filter @nocturne/database world:grant-role --",
    "auth:generate": "pnpm --filter @nocturne/auth auth:generate",
    "auth:migrate": "pnpm --filter @nocturne/auth auth:migrate",
    "nocturne:cli": "tsx scripts/nocturne-cli.ts",
    "nocturne:agent": "tsx scripts/nocturne-agent.ts",
    "nocturne:scenario": "tsx scripts/nocturne-scenario.ts",
    "start:mcp": "node apps/mcp/dist/index.cjs",
    "test:cli": "vitest run test/cli.test.ts"
  },
  "devDependencies": {
    "@nocturne/agent-sdk": "workspace:*",
    "@types/node": "^22.0.0",
    "prettier": "^3.5.0",
    "tsup": "^8.5.0",
    "tsx": "^4.19.0",
    "turbo": "^2.5.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
