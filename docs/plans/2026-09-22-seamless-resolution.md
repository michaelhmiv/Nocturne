# Seamless category resolution

Player language is mapped to a category + selector, then bound against the world.
Clarification is allowed only when two materialized instances would mutate
different entities. Missing grocery-like nouns are category destinations, not
form errors.

See `packages/contracts/src/category-resolution.ts`.
