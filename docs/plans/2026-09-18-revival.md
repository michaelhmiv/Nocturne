# Nocturne revival: accepted product contract and implementation register

The standalone website is the primary client: conversational play with status, progression, map, and live city news. Real NYC geography contains fictional people and businesses. Ordinary humans can attempt arbitrary plausible actions; superpowers are excluded. ChatGPT/MCP remains an optional client of the same authoritative API.

The core loop is explore/interact, earn/spend, and cause/respond to trouble. Skills, assets, and reputation provide progression. Elections, cosmetics, and autonomous NPC ambitions are deferred. NPCs are stationary but reactive, including witnesses, merchants, and incident-triggered police.

Characters and property remain vulnerable offline. Defeat uses accelerated GTA-style recovery, while theft, damage, legal consequences, and reputation persist. The social preference approach/keep-to-self controls voluntary social contact, never immunity. Encounters depend on location, routes, visibility, and access. Missing response windows use a standing self-preservation policy (escape when feasible, surrender when cornered), not an automatic fight to the death.

Only reported/public evidence reaches the newspaper. An unidentified robbery can be reported without identifying its offender; undiscovered crimes remain unpublished. Narration, status, map, history, and other players must agree with committed state.

## Ordered PRs and current status

| PR  | Scope                                   | Acceptance evidence                                         | Status      |
| --- | --------------------------------------- | ----------------------------------------------------------- | ----------- |
| 1   | Product contract and baseline           | Current main, open PRs, test inventory                      | In progress |
| 2   | Actions certification foundation        | Real auth, web/API/worker/PostgreSQL, artifacts             | Pending     |
| 3   | Durable actions and consequences        | Restart, race, retry, failure without mutation              | Pending     |
| 4   | Jev decision service and evaluation     | Real interface, held-out cases, shadow results              | Pending     |
| 5   | Canonical NYC geography and travel      | Connected locations, route/position agreement               | Pending     |
| 6   | Chat/status/map website                 | Mobile/desktop visual and browser verification              | Pending     |
| 7   | Exploration and reactive interactions   | Persistent discoveries and open-ended actions               | Pending     |
| 8   | Economy and progression                 | Atomic exchanges, reconciled resources                      | Pending     |
| 9   | Shared encounters                       | Two-player interception, response windows, offline behavior | Pending     |
| 10  | Crime and recovery                      | Evidence-based reactions and accelerated recovery           | Pending     |
| 11  | Destruction and offline vulnerability   | Durable area effects, repairs, occupant consequences        | Pending     |
| 12  | Newspaper                               | Public evidence only, corrections, deduplication            | Pending     |
| 13  | Optimization and adversarial evaluation | Measured end-to-end latency and semantic quality            | Pending     |
| 14  | Deployed certification                  | Exact revision, migration upgrade, restore, staging         | Pending     |

## AI authority

OpenRouter is the user-authorized model gateway. The live evaluation discovers model availability rather than assuming every provider/model is present. Jev is intended for bounded choices, reference matching, plausibility and reactions; generative models retain complex proposals and narration. Jev availability and typed-interface support must be verified before activation. Never present another model as Jev.

Backend validation owns inventory, ownership, money, physical state, clocks, rolls, and atomic commits. Decision confidence is not game success probability. No AI decision, however confident, bypasses invariants. The initial diagnostic model comparison is not sufficient for release certification.

## Test policy

GitHub Actions is mandatory. Deterministic tests require no paid key. Live evaluation requires OPENROUTER_API_KEY and reports missing access as blocked/failed, not passed. Secret-bearing tests execute trusted code only. Every behavior needs player-visible and authoritative assertions, and failures retain sanitized logs/traces/screenshots. The old manual-only and MCP-primary documents are superseded by this contract.

## Verified foundation evidence — 2026-09-18

The original live-provider failures were configuration failures rather than evidence that the stored credential was unavailable. The provider-contract workflow was still selecting the direct DeepSeek provider while `OPENROUTER_API_KEY` was the available credential, and the first evaluation workflow passed an empty model list. Both conditions are now explicit failures rather than silent skips.

A trusted live provider-contract run passed through OpenRouter with `google/gemini-3.7-flash` as the configured authoritative, creative, and planner model:

- AI Provider Contract run 35376503823: passed with three live OpenRouter request IDs.
- Corrected AI Provider Contract run 35377468507: passed after the Jev evaluation transport change.

Jev is not a chat-completions model in this integration. The first direct alias probe through `/chat/completions` produced 32 HTTP 400 responses. The corrected diagnostic uses OpenRouter's Decisions endpoint with typed choice questions. Run 35377468477 passed and resolved `~typesafe/jev-latest` to `typesafe/jev-1.13-20260917`.

The diagnostic corpus is deliberately small and is not a release gate:

| Requested model                   | Valid decisions | Correct | Accuracy | False acceptances |    p50 |     p95 | Reported cost |
| --------------------------------- | --------------: | ------: | -------: | ----------------: | -----: | ------: | ------------: |
| `~typesafe/jev-latest`            |           32/32 |   25/32 |   78.13% |                 2 | 223 ms |  385 ms |     $0.001125 |
| `google/gemini-3.7-flash`         |           32/32 |   30/32 |   93.75% |                 0 | 2.94 s |  5.11 s |     $0.021772 |
| `~deepseek/deepseek-flash-latest` |           24/32 |   17/32 |   53.13% |                 0 | 3.52 s | 39.45 s |     $0.007512 |

Both Jev false acceptances were repetitions of the same inaccessible-item case: the actor was at a bank while the requested suitcase was at home two miles away. Location, access, inventory, balance, quantity, and other authoritative prerequisites therefore remain deterministic backend checks. The current evidence supports evaluating Jev for fast bounded classification, candidate/reference matching, and shadow decisions; it does not support replacing the backend validator or promoting Jev as the general generative model.

On the same human commit used for the corrected Jev transport, production build, typecheck, unit/telemetry, database, compiled API integration, browser action matrix, and all eight action-matrix shards passed. The only certification failure was formatting of the evaluation script; the repository autofix applied only that formatting change in the following commit.
