name: AI Provider Contract

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: "17 9 * * *"

permissions:
  contents: read

concurrency:
  group: ai-provider-contract-${{ github.ref }}
  cancel-in-progress: true

jobs:
  provider-contract:
    name: Provider Contract / Production Planner
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      AI_PROVIDER: ${{ vars.AI_PROVIDER || 'deepseek' }}
      AI_MODEL: ${{ vars.AI_MODEL || 'deepseek-v4-flash' }}
      AI_AUTHORITATIVE_MODEL: ${{ vars.AI_AUTHORITATIVE_MODEL || vars.AI_MODEL || 'deepseek-v4-flash' }}
      AI_CREATIVE_MODEL: ${{ vars.AI_CREATIVE_MODEL || vars.AI_MODEL || 'deepseek-v4-flash' }}
      AI_BASE_URL: ${{ vars.AI_BASE_URL || 'https://api.deepseek.com' }}
      AI_THINKING_MODE: ${{ vars.AI_THINKING_MODE || 'disabled' }}
      AI_JSON_MODE: ${{ vars.AI_JSON_MODE || 'true' }}
      AI_SEND_TEMPERATURE: ${{ vars.AI_SEND_TEMPERATURE || 'true' }}
      AI_MAX_TOKENS: ${{ vars.AI_MAX_TOKENS || '4096' }}
      AI_TIMEOUT_MS: ${{ vars.AI_TIMEOUT_MS || '60000' }}
      AI_EXTRA_BODY_JSON: ${{ vars.AI_EXTRA_BODY_JSON || '{}' }}
      AI_API_KEY: ${{ secrets.AI_API_KEY }}
      DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11.17.0
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Check live-provider credential availability
        id: configuration
        shell: bash
        run: |
          if [ -n "$AI_API_KEY" ] || [ -n "$DEEPSEEK_API_KEY" ] || [ -n "$OPENAI_API_KEY" ] || [ -n "$OPENROUTER_API_KEY" ]; then
            echo "enabled=true" >> "$GITHUB_OUTPUT"
          else
            echo "::notice::No provider credential is stored in GitHub. The required deterministic provider-resilience tests still run in Nocturne Certification, and the deployed Railway environment is exercised by Production Deployment Smoke."
            echo "enabled=false" >> "$GITHUB_OUTPUT"
          fi
      - name: Verify configured provider, model, and production planner schema
        if: steps.configuration.outputs.enabled == 'true'
        run: |
          mkdir -p artifacts
          set -o pipefail
          pnpm --filter @nocturne/ai-gm test:provider-contract 2>&1 | tee artifacts/provider-contract.log
      - name: Record delegated production validation
        if: steps.configuration.outputs.enabled != 'true'
        run: |
          mkdir -p artifacts
          echo "Live provider validation is delegated to the mandatory post-deployment gameplay smoke running against Railway's configured provider." | tee artifacts/provider-contract-delegated.log
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: provider-contract-${{ github.run_id }}
          path: artifacts/
          retention-days: 30
